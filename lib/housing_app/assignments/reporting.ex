defmodule HousingApp.Assignments.Reporting do
  @moduledoc false

  # import Ecto.Query, only: [from: 2]

  def bookings(actor: _current_user_tenant, tenant: _tenant) do
    #   query =
    #     from booking in HousingApp.Assignments.Booking,
    #       where: booking.tenant_id == ^tenant.id

    #   query
    []
  end
end
